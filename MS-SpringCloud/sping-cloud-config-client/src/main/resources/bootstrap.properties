#profile must be in bootstrap file. or use yml.
#If in application properties, wont be effective and default will be loaded
spring.profiles.active=dev
spring.cloud.config.uri=http://localhost:9001
spring.cloud.config.username=testuser
spring.cloud.config.password=testuser
#if encrypt.key not included and server encrypt.enabled=false, app wont come up. 
encrypt.key=testencryption

